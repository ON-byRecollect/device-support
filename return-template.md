# 返品案内 共通メッセージのひな形

`return-admin.html` の各入力欄に貼り付けて使う文面のたたき台です。実際の運用に合わせて調整してください。

---

## 冒頭のあいさつ（intro）

```
この度はご期待に沿えず、申し訳ございませんでした。
以下の手順にそって、商品のご返送をお願いいたします。
```

## 共通メッセージ（common）

```
・返送は、このご案内をお送りしてから7日以内にお願いいたします。
・商品本体に加え、付属品・箱・ケーブル類もすべて揃えてご返送ください。
・返送時の輸送中の破損を防ぐため、緩衝材で包んでいただけますと幸いです。
・返金は、商品の到着と状態確認ののち、5営業日以内に手続きいたします。
```

## 返送の手順（steps／1行1ステップ）

```
下のバーコードを印刷、またはこの画面をスマートフォンに表示できるようにしてください。
最寄りのコンビニエンスストア、または配送業者の窓口にお持ちください。
レジや窓口でバーコードを提示し、発送手続きを行ってください（送料の負担はありません）。
発送後に受け取る控えを保管し、記載の追跡番号を取引メッセージでお知らせください。
```

## 強調したい注意事項（caution／任意）

```
バーコードの有効期限は、発行から14日間です。期限を過ぎた場合は、お手数ですが再発行をご依頼ください。
```

---

# 運用の手順（管理者向け）

## 1回の返品対応でやること

1. **バーコード画像を用意する** … 配送業者の伝票バーコードを画像として書き出す（従来通り）
2. **`return-admin.html` を開く**（自分の端末で開くだけ。ネットに公開する必要はありません）
3. 各欄を入力し、バーコード画像を選び、**パスコードを「自動生成」**する
4. **「暗号化してファイルを作成」** を押す
5. **「ファイルを保存」** で `◯◯◯◯.json` をダウンロード
6. その `.json` を、リポジトリの **`return/` フォルダに置いてコミット**する
7. 画面に出た **リンクとパスコードを、取引メッセージで購入者本人に伝える**

## 購入者がやること

1. 届いたリンクを開く
2. 届いたパスコードを入力する
3. 案内とバーコードが表示される → スクリーンショットを保存して発送

---

# セキュリティについて（正しく理解しておくための整理）

この仕組みで**守れること**

- リンク（URL）だけが第三者に漏れても、**パスコードがなければ中身は一切読めません**。ファイルは暗号化されており、開いても意味のないデータに見えます。
- リポジトリのファイル一覧を見られても、バーコード画像は**単体では存在しません**（暗号化データの中に封じ込められています）。ファイル名もランダムで、誰のものか分かりません。
- サイトのソースコードを全部読まれても、**パスコードはどこにも書かれていません**。

この仕組みで**守れないこと**（運用でカバーする部分）

- **パスコードとリンクの両方**が同じ経路で第三者に渡ってしまうと、読まれます。できれば別々の手段で、少なくとも本人にだけ伝えてください。
- 購入者本人が、復号後の画面やスクリーンショットを第三者に渡した場合は防げません。

技術で守れる範囲はこれでほぼ最大です。最後の砦は「パスコードを本人にだけ伝える」という運用になります。

---

# ファイル構成（返品機能の追加分）

```
return.html          … 購入者が開くページ（パスコード入力→復号表示）
return-admin.html    … 管理者が暗号化ファイルを作るページ
return/              … 暗号化済みの .json を置くフォルダ（購入者ごとに1つ）
return-template.md   … このファイル（運用メモ）
```

トラブルシューティング本体（`index.html` / `flow.js` / `admin.html`）とは独立しています。返品機能を足しても、既存の動作には影響しません。
